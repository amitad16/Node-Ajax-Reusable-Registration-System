nav.navbar.navbar-expand-lg.navbar-light.bg-light
    if (user)
        a.navbar-brand(href='/users/' + user.username) FullRegSystem
    else
        a.navbar-brand(href='/') FullRegSystem
    button.navbar-toggler(type='button', data-toggle='collapse', data-target='#toggle',
    aria-controls='navbarSupportedContent', aria-expanded='false', aria-label='Toggle navigation')
        span.navbar-toggler-icon
    #toggle.collapse.navbar-collapse
        ul.navbar-nav
            if (user)
                //p= title
                //p= title==='Dashboard' && 'Home' ? 'active' : undefined
                li.nav-item(class=title==='Dashboard' && 'Home' ? 'active' : undefined)
                    a.nav-link(href='/users/' + user.username) Home
            else
                li.nav-item(class=title==='Home' ? 'active' : undefined)
                    a.nav-link(href='/') Home
            li.nav-item(class=title === 'link1' ? 'active' : undefined)
                a.nav-link(href='#') Link1
            li.nav-item(class=title === 'link2' ? 'active' : undefined)
                a.nav-link(href='#') Link2
        ul.navbar-nav.ml-auto
            if (user)
                li.nav-item.dropdown
                    a#manageDropdown.nav-link.dropdown-toggle(href='#', role='button', data-toggle='dropdown', aria-haspopup='true', aria-expanded='false')
                        | #{user.email}
                    .dropdown-menu(aria-labelledby='manageDropdown')
                        a.dropdown-item(href='/users/' + user.username + '/profile') Profile
                        a.dropdown-item(href='/users/' + user.username + '/settings/profile') Settings
                        .dropdown-divider
                        a.dropdown-item(href='/user/logout') Logout
            else
                li.nav-item(class=title==='Login' ? 'active' : undefined)
                    a.nav-link(href='/user/login') Login
                li.nav-item(class=title==='Register' ? 'active' : undefined)
                    a.nav-link(href='/user/register') Register